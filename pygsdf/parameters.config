
[project_setting]

proj_name = TA
component = LHZ
lalim = 25 50
lolim = -125 -65
gridsize = 0.3
periods = 20 25 32 40 50 60 80 100

[window_setting]

largest_epidist_range = 3000
cycle_before = 2
cycle_after = 5
min_dist_tol = deg2km(20)
max_dist_tol = deg2km(160)
min_groupv = 2
max_groupv = 5
cent_freq = 0.025

[xcorr_setting]

minstadist = 5
maxstadist = 200
is_rm_resp = 0
refv = 4
# refphv = ones(size(parameters.periods))*4
min_width = 0.06
max_width = 0.10
wintaperlength = 30
prefilter =  10 200
xcor_win_halflength = 150
# xcor_win_iter = zeros(size(parameters.periods))
Nfit = 2
Ncircle = 5
cohere_tol = 0.5
tp_tol = 10

#===========================================# work later!
# Tomography parameters
# smweight_array = 3*[0.4 0.3 0.2 0.2 0.2 0.5 1 2];  % smoothing weight for the deltaSx and delta Sy
# raydensetol=deg2km(parameters.gridsize)*2;
# Tdumpweight = 0;  % dumping the ray to the girgle circle path
# Rdumpweight = 0;  % dumping the region to have the same phase velocity
# fiterrtol = 3;   % error allowed in the wavelet fitting
# isRsmooth = 1;  % smoothing due to Sx and Sy or Sr and S_theta
# dterrtol = 2;    % largest variance of the inversion error allowed
# inverse_err_tol = 2;  % count be number of standard devition
# min_amp_tol = 0.1;  % station with amplitude smaller than this ratio of average amplitude will not be used.
# amp_var_tol = 2; % how much times change of amplitude of single station to the mean value of nearby stations should be considered as bad measurement
# alpha_range = [1 1];
# alpha_search_grid = 0.1;

# stacking parameters
# min_csgoodratio=[3 3 3 3 5 10 15 15]; % minimum radio between good and bad measurements for a good event
# min_phv_tol = 3;
# max_phv_tol = 5;
# is_raydense_weight = 1;
# min_event_num = 10;
# err_std_tol = 2;
# issmoothmap = 1;
# smooth_wavelength = 0.25;
# event_bias_tol = 2;

# parameters for azimuthal anisotropy inversion
# beta version
# smsize = 1;  % averaging nearby grid number
# off_azi_tol = 30; % differ from great circle path
# is_one_phi = 1;


[error_codes]

low_cohere = -1
high_tp_err = -2
cs_fit_error = -3
sta_fit_error = -4
sta_outofrange = -5
sta_lackdata = -6
sta_outofepidist = -7
xcor_outofwin = -8
init_CS_struct = -10

